import {
  SHOW_LOGIN_MODAL,
  HIDE_LOGIN_MODAL,
  SHOW_SIGNUP_MODAL,
  HIDE_SIGNUP_MODAL,
} from '../../modules/actionTypes.js';

export default function homeReducer(
    state = {}, action) {
  switch (action.type) {
    default:
      return state;
  }
};
